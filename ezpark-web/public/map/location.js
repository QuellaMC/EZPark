class Location {
    constructor(x, y, scale, infoKey, url, embedUrl) {
        this.x = x;
        this.y = y;
        this.scale = scale;
        this.infoKey = infoKey;
        this.url = url;
        this.embedUrl = embedUrl;
    }
}

export { Location };